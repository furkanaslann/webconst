---
interface Props {
  className?: string;
  bgColor?: 'white' | 'gray' | 'gray-600' | 'primary';
}

const {
  className = '',
  bgColor = 'white',
} = Astro.props;

const bgClasses = {
  white: 'bg-white',
  gray: 'bg-gray-50',
  'gray-600': 'bg-gray-600',
  primary: 'bg-primary-50',
};
---

<section class={`${bgClasses[bgColor]} ${className}`}>
  <slot />
</section>
